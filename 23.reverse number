#include<stdio.h>
int  main()
{
	printf("swaroop-192211969\n");
	int n,sum=0,rem;
	printf("enter the number");
	scanf("%d",&n);
	while(n>0)
	{
		rem=n%10;
		sum=sum*10+rem;
		n=n/10;
	}
	printf("%d",sum);
}
